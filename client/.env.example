# Leave blank to use Vite's dev proxy (see vite.config.ts) or same-origin requests in production.
# Set to your deployed API origin if the client and server are hosted separately, e.g.:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
